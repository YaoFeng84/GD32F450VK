0、目前驱动库晶振参数
    0.1、外部高速晶振频率：25MHz，主频：168MHz
    0.2、外部低速晶振频率：32.768kHz

1、晶振相关的修改,需要修改配置以下文件:
    1.1、GD32f4xx.h第61行外部高速晶振频率
    1.2、GD32f4xx.h第86行外部低速晶振频率
    1.3、在system_gd32f4xx.c中注释掉45~52行，否则第57行将不起作用。
    1.4、system_gd32f4xx.c第57行选择，配置为主频200MHz。
    1.5、在外部晶振为25MHz时，如果有使用USB模块，则主频最高只能到168MHz，否则PLL无法精确生成48MHz的USB时钟。
    1.6、外部晶振为8MHz时，如果有使用USB模块，则主频最高可以到192MHz，也可以精确生成48MHz的USB时钟。
         PLL_M = 1(预分频)，PLL_N = 48(倍频系数)，PLL_P = 2(分频输出P)，PLL_Q = 8(分频输出Q)。
         PLL_VCO_OUT=8×48=384MHz（PLL_VCO_OUT在100~432MHz范围内）
         CK_SYS=384/2?=192MHz,USB_CLK=384/8?=48MHz

2、模块相关的开启和关闭,需要修改配置以下文件:
    2.1、

3、使用时,直接将CommHead这个文件夹及相应的DRI级的.h文件拷贝到工程中即可。
